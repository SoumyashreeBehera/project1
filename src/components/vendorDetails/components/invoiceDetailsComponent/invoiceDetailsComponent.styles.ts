import { createStyles } from "@mantine/core";

const useStyles = createStyles(() => {
  return {
    subTitle: {
      fontSize: 20,
      fontWeight: 600,
    },
    dollarIconBox: {
      padding: "0px 12px",
      background: "#E7EAEE",
      height: "100%",
      alignItems: "center",
      marginLeft: -12,
    },
  };
});

export default useStyles;
